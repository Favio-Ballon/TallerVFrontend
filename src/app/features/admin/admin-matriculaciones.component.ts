// DELETED: Matriculaciones UI moved to `src/app/features/docentes/admin-matriculaciones.component.*`
// Kept as an inert placeholder file to avoid accidental imports. Remove from repo if you want a hard delete.

/* intentionally empty */
